# ~/.config/shell/aliases.sh - aliases shared by every POSIX shell.
#
# GENERATED by chezmoi from .chezmoidata/shell.yaml - edit that, then
# `chezmoi apply`. Changes made here are overwritten.

{{ range $name, $command := .shell.aliases -}}
alias {{ $name }}='{{ $command }}'
{{ end -}}
